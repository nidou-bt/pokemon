generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Pokemon {
  id        String @id @default(auto()) @map("_id") @db.ObjectId
  name      String
  pokemonId BigInt
  spriteUrl String
}
